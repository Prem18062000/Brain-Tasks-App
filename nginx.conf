server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html index.htm;

    # For React/Vite single-page application routing
    location / {
        try_files $uri $uri/ /index.html;
    }

    error_page 404 /index.html;
}
